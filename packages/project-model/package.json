{
  "name": "@hyperframes/project-model",
  "version": "0.1.0",
  "private": true,
  "description": "Shared project envelope, document kinds, stable identities, command validation and atomic revision history (contracts C01, C03, C04, C05).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "echo \"no build step yet (AFM-017)\" && exit 0",
    "test": "echo \"no tests yet (AFM-017)\" && exit 0",
    "typecheck": "tsc --noEmit"
  }
}
