{
  "name": "@hyperframes/diagram-motion",
  "version": "0.1.0",
  "private": true,
  "description": "Compiles guided-view semantic intent into deterministic, seekable motion on the composition timeline (contract C06).",
  "files": [
    "dist",
    "src"
  ],
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "node": "./dist/index.js",
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hyperframes/core": "workspace:*",
    "@hyperframes/diagram-engine": "workspace:*",
    "@hyperframes/project-model": "workspace:*",
    "saxes": "6.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0"
  }
}
