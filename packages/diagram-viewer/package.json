{
  "name": "@hyperframes/diagram-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone diagram viewer shell: the HTML template and viewer assets that wrap a compiled diagram artifact.",
  "files": [
    "src",
    "assets",
    "scripts"
  ],
  "type": "module",
  "exports": {
    ".": "./src/index.mjs",
    "./template": "./assets/template.html",
    "./assets/*": "./assets/*",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "echo \"no tests yet (AFM-030)\" && exit 0",
    "build": "node scripts/verify-assets.mjs"
  },
  "hyperframesWorkspaceContract": {
    "typecheck": {
      "optOut": true,
      "reason": "The viewer is a single hand-written .mjs bundle with no TypeScript sources and no declaration file, so tsc has nothing to check. Its contract is verified by scripts/verify-assets.mjs at build time."
    }
  }
}
